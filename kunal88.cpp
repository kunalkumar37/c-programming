#include<iostream>
using namespace std;
int main()
{
    struct Graph *adjmatrixofgraph()
    {
        int i,u,v;
        struct Graph *G=(struct Graph *)malloc(sozeof(struct Graph));
        if(!G)
        
        {
            printf("memory error");
            return ;


        }
        scanf("number of vertices :%d ,number of vertices is %d",&G->V,&G->E);
        G->Adj =malloc(sizeof(G->V *G->V));
        for(u=0;u<g->V)

    }
}